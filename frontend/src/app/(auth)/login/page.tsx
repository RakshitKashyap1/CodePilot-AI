import { LoginForm } from "@/features/auth/components/login-form";

export const metadata = {
  title: "Login | CodePilot AI",
  description: "Login to your CodePilot AI account",
};

export default function LoginPage() {
  return <LoginForm />;
}
